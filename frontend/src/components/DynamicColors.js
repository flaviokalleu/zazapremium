import useDynamicColors from '../hooks/useDynamicColors';

const DynamicColors = () => {
  useDynamicColors(); // Aplicar cores dinâmicas
  return null; // Este componente não renderiza nada
};

export default DynamicColors;
