export const initSession = async (req, res) => {
  res.status(410).json({ error: 'whatsapp-web.js removido. Use /api/baileys' });
};

export const sendTextMessage = async (req, res) => {
  res.status(410).json({ error: 'whatsapp-web.js removido. Use /api/baileys' });
};

export const sendMediaMessage = async (req, res) => {
  res.status(410).json({ error: 'whatsapp-web.js removido. Use /api/baileys' });
};
